{
  "name": "@dillon-os/scroll-engine",
  "version": "0.1.0",
  "description": "Composited scroll-world engine for Prospect Radar homepage concepts. Extracted byte-identical from the next10 batch (refs/pull/270/head). Spec-driven: one generic Three.js engine, per-site declarative configs.",
  "private": true,
  "type": "module",
  "scripts": {
    "textures": "node scripts/generate-textures.mjs",
    "pages": "node scripts/build-pages.mjs",
    "build": "npm run textures && npm run pages && vite build",
    "qa": "node scripts/qa.mjs",
    "qa:browser": "node scripts/qa-browser.mjs"
  },
  "dependencies": {
    "three": "^0.179.1"
  },
  "devDependencies": {
    "playwright": "^1.54.2",
    "vite": "^7.1.1"
  }
}
