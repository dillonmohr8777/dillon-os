# With Not For AI — Apple-Inspired Homepage Concept

Private, source-backed homepage rebuild for review before the Jack Lesser call.

## Preview

Serve the `site` directory over loopback. On this workstation:

```powershell
& 'C:\Users\dillo\.codex\tools\Start-CodexLocalPreview.ps1' -Path '.\site' -Json
```

The current review server is loopback-only at `http://127.0.0.1:51302/` while its helper process remains active. The QA runner also accepts a preview URL as its first argument or through `WNF_QA_URL` when the helper assigns a different port.

## Files

- `site/index.html` — semantic page and exact source-backed copy.
- `site/styles.css` — responsive layout, launch choreography, and reduced-motion mode.
- `site/overdrive.css` — five-state opening, liquid-glass system, robot product theatre, hand composition, and particle finale.
- `site/app.js` and `site/overdrive.js` — opening playback, robot blink/nod/pointer behavior, scroll scenes, menu, and briefing tabs.
- `site/particle-logo.js` — deterministic alpha-aware assembly of the exact official WNF logo.
- `PRODUCT.md` — strategic source of truth.
- `DESIGN.md` — scanned implementation design system.
- `SOURCE-SPEC.md` and `PROVENANCE.json` — source and asset authority.
- `QA.md`, `qa/report.json`, and `qa/contact-sheets/opening-overdrive.png` — current verification evidence.

## State

Private and noindexed. Do not deploy or contact the prospect without explicit approval.
