# Copy to .env and fill in — NEVER commit the real token.
# Create the token in HubSpot: Settings > Integrations > Private Apps > Create.
# Required scope: "content" (Content Operations — manage blog posts + pages).
# Token format looks like: pat-na2-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
HUBSPOT_PRIVATE_APP_TOKEN=
