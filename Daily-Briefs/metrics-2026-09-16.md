---
tags: [brief, metrics, vault-health]
date: 2026-09-16
note_type: metrics-snapshot
---

# Vault metrics — 2026-09-16

Diffed against `Daily-Briefs/metrics-2026-09-15.md` (previous snapshot,
1-day gap) — but see the branch-catch-up note below before trusting the raw
deltas.

## Vital signs

| Metric | Value | Delta (vs 2026-09-15) |
| --- | --- | --- |
| Total notes (vault-wide `.md`, incl. hidden dirs) | 1,329 | +515 (branch catch-up, not organic — see below) |
| Notes touched in last 7 days (git-committed) | 1,253 | +1,153 (branch catch-up + genuine radar-sweep churn — see below) |
| Notes created in last 7 days (git-added) | 1,253 | +1,193 (same cause) |
| Open tasks `- [ ]` | 378 | +11 |
| Completed tasks `- [x]` | 100 | +74 |
| Task completion rate | 20.9% (100/478) | +14.3pp |
| Clients in 01_Clients/ (dirs + standalone pages, excl. Client Index) | 40 | +3 |
| Clients touched <48h | 1 (Omega Landscaping, by git evidence — frontmatter stale) | +1 |
| Clients touched <7d (2-7d watch band) | 0 | +0 |
| Clients stalled (>7d by `last_touched`) | 39 | +2 |
| Content library notes in 03_Content/ | 7 | +0 |
| Content pieces flagged ship-ready | 0 | +0 |
| Sessions logged in 10_Sessions/ (last 7d, git) | 8 (only 1 — "2026-09-14 Weekend accounting" — is a genuine new session; the other 7 are old files carried in by the branch merge) | +8 |
| Inbox depth (00_Inbox/) | 93 | +70 |
| goal_current (OS Config, active clients) | 12 (stale — see below) | +0 |
| goal_target | 100 | +0 |

**Branch catch-up, read this before the deltas above:** this session started
with local `main` detached and 60 commits behind `origin/main` (last synced
2026-08-18-ish per a prior merge commit's own message). The first action this
run was fast-forwarding `main` to `origin/main`'s current head
(`42ae1b66`). That single catch-up is why total notes jumped from 814→1,329
and "touched/created in 7 days" jumped into the thousands — it is
`git log --since` correctly counting real commits that happened over the
last real 7 days across ~81 commits (verified: `git log --since="7 days
ago" --oneline | wc -l` = 81), most of it legitimate radar-sweep/site-factory
automation rewriting large batches of tracked site files, not editorial or
client-note activity. Excluding merge commits doesn't change the touched
count (still 1,253), confirming this is real commit volume, not a merge
artifact double-count. Treat yesterday's 814/100 baseline as understated
(computed on a stale, 60-commits-behind branch), not today's number as a
true one-day spike.

Client count (40, +3 vs. yesterday's 37) is real: the catch-up brought in
`Nexla`, `Puttery NYC`, and `Deborah-Mara` as new client folders (none carry
`last_touched` frontmatter yet — logged as a data gap in `pulse-today.md`).

Method notes (unchanged from prior snapshots): client staleness buckets use
`last_touched` frontmatter, not raw file mtime — git-committed "touch" dates
on `01_Clients/` are contaminated by bulk infra/branch merges that carry old
content forward without editing it (see `pulse-today.md` for the full
breakdown and the one exception, Omega Landscaping, which has real
2026-09-14 content but stale frontmatter).

`System/OS Config.md` still reads `goal_current: 12`, `last_updated:
2026-07-12` — 66 days stale, unchanged. `01_Clients/Client Index.md` still
carries `canonical_active_route_count: 23` with
`registry_reconciliation_required: 2` (Capsule & Tonic, Everyday Life
Insurance). **New finding this snapshot:** the canonical registry file
itself (`/home/user/client-operations-canonical/registry/clients.json`,
`generatedAt: 2026-08-07`) lists 23 "active" clients, but that list still
includes `align-hcm` (employment **ended, confirmed 2026-09-02** per
`CLAUDE.md`) and `fagan-painting` (retired 2026-09-05 per vault commit
`d623bf5a`) — both should not count, and `momentum-360` is explicitly
excluded from the client count by the goal's own definition (agency layer,
not a client). Corrected estimate: **23 − align-hcm − fagan-painting −
momentum-360 = 20 active per the registry**, and the registry itself is 40
days stale so this is a floor, not a final number. Neither `12` nor `23` is
correct as-is; recommend a fresh registry pull before the next planning
cycle rather than trusting either cached figure. OS Config was not edited
(out of scope for a metrics-only pull).

## Workload mix (predict-work.js)

Ran `node _os/automation/bin/predict-work.js --lookahead-days 35
--history-days 90 --client-ops-root /home/user/client-operations-canonical`.
The `--client-ops-root` flag is required in this sandbox (confirmed again by
a first pass without it: `client_operations_root_available: false`, 1
candidate). **Status: ok, not degraded** — `client_operations_root_available:
true`, `canonical_queue_available: true`, source
`client-operations://queue/work-items.json`, fingerprint
`de4391c869f67484ec6254e1a7a7c9a07e2dbea3e3c6991bd84311538806f7f4`, generated
2026-09-16T11:13:19Z. Output: `Daily-Briefs/predicted-work-2026-09-16.{json,md}`,
state at `12_Brain/state/work-predictor/latest.json`.

- **Dated packages observed (90-day window, 2026-06-19 → 2026-09-16):** 31
  deliverable events (`deliverable_events_scanned: 31`), up from 28 — a new
  historical event entered the trailing 90-day window.
- **Counts by work-package type (top-candidate stack, 6 total):**
  `bok-weekly-designed-content-kit` ×1 (confirmed-pattern, 0.96),
  `website-design-build` ×1 (canonical-queue, 0.86),
  `workflow-automation-system` ×1 (canonical-queue, 0.68),
  `data-source-integration` ×2 (canonical-queue, 0.68 each — momentum-360,
  align-hcm), `research-audit-decision-brief` ×1 (canonical-queue, 0.68).
  Full per-event historical classification across all 31 deliverables was not
  independently reclassified this run (unchanged practice from prior
  snapshots — that read lives in the predictor's own internal state, not
  re-derived here).
- **Classifier coverage:** `queue_candidates_used: 5`,
  `stale_queue_items_excluded: 11` out of 16 active queue items scanned —
  unchanged from yesterday.
- **Align HCM caveat:** the `align-hcm` "watch" candidate (reauth Align
  HubSpot/Microsoft) is itself a reconciliation flag, not necessarily live
  work — see the roster-mismatch note in `pulse-today.md`. It should not be
  treated as a real preparation item until the canonical queue closes or
  confirms it against the 2026-09-02 end date.
- **Latest Chronos receipt:** `EXP-DILLON-WORKLOAD-CHRONOS-SHADOW-20260902`,
  decision **retain-deterministic-baseline-primary**. Gates all still false
  (`single_holdout_beats_best_baseline`, `single_holdout_quantile_calibration`,
  `repeated_holdouts`, `planner_consumption`). Its fingerprint
  (`d58cc5d6…`, 2026-09-02) still does not match today's run
  (`de4391c8…`), now 14 days stale — **not attached as live guidance, not
  used for ranking or as a capacity commitment.** Chronos was not re-run
  from this pull per instructions.

## Observations

- **Trending well:** task completion rate jumped to 20.9% (100/478) from
  6.6% — real, driven by the branch catch-up surfacing 74 more completed
  tasks that existed on `origin/main` but were invisible to this checkout's
  stale branch. The canonical-queue read stayed stable and correctly
  provenanced with the explicit `--client-ops-root` flag.
- **Decaying:** the OS-Config/Client-Index/registry three-way client-count
  gap (12 vs. 23 vs. a registry that still counts two ended relationships)
  has now sat unresolved across ten-plus consecutive snapshots, and today's
  dig into the registry file itself shows the "23" figure was never clean —
  it's carrying stale entries too.
- **Concrete fix:** run a real registry refresh (or a manual reconciliation
  note) that removes `align-hcm` and `fagan-painting` from the active count
  and settles on one canonical `goal_current` figure — three different
  numbers on the vault's own headline "ROAD TO 100 CLIENTS" metric is the
  actionable item, not a new one but now with a concrete corrected floor
  (20) to work from instead of an unexplained gap.

## Data gaps

- No per-piece ship-ready status field exists in `03_Content/` — still 0/7
  by absence of a status marker (only 2 of 7 notes carry `status:` at all,
  both `active`). Unchanged.
- Chronos receipt fingerprint mismatch persists (14 days stale) — no live
  Chronos read attached; workload-mix figures come from the deterministic
  router only.
- Three new client folders (Nexla, Puttery NYC, Deborah-Mara) carry no
  `last_touched`/`due`/`next_action` frontmatter — invisible to staleness
  bucketing until backfilled. Deborah-Mara's folder also lacks an
  `overview.md` (has an AEO-dated README instead) — schema drift from the
  other client folders.
- Client staleness buckets reflect vault frontmatter/git history only; real
  client work done outside tracked files (calls, ad platforms, live
  email/Slack) stays invisible here — the canonical queue read is the only
  signal for that, and it names 6 of 40 clients with live (mostly blocked)
  work.
- The canonical client registry snapshot mounted in this sandbox is itself
  40 days stale (`generatedAt: 2026-08-07`) and predates both the Align HCM
  end (2026-09-02) and the Fagan Painting/Shadow HVAC retirement
  (2026-09-05) — any count pulled from it needs the same correction applied
  here until it's regenerated.
