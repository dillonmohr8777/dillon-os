---
name: vault-protocol
description: Dillon OS agent protocol. Search, source, compile. Approval-gated send.
---

# Vault protocol

Read INDEX.md, System/operating-status.md, System/approval-queue.md, then the linked note.

- Captures are immutable. Compile into numbered 12_Brain folders.
- Every durable claim needs a source. Missing source → unverified.
- Do not send, publish, deploy, spend, or merge without explicit operator approval.
- Keep Align HCM (full-time) separate from client revenue.
- Two outputs: the artifact, plus the smallest useful vault update when knowledge was created.
