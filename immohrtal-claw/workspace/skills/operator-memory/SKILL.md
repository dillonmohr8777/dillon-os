---
name: operator-memory
description: Pin durable operator facts. Daily tape for the rest.
---

# Operator memory

- `memory_search` before answering personal preferences or prior pins.
- `memory_write` with `pin: true` for facts that should land in MEMORY.md.
- Do not pin secrets, passwords, tokens, or one-time codes.
- Routine chatter stays off MEMORY.md.
