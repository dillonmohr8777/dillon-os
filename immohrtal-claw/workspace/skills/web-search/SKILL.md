---
name: web-search
description: Search and fetch the public web before guessing facts.
---

# Web search

Use `web_search` for current public facts. Use `web_fetch` only on a specific
http(s) URL the operator named or a search result returned.

Do not fetch localhost, private IPs, or file URLs. If fetch fails, say so.
