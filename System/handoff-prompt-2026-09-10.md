# Master Handoff Prompt — 2026-09-10 (Opus 5 Max Edition)

Copy the block below into Claude Opus 5 Max, Cursor, or any fresh session on this machine. It is completely self-contained, verified against live state, and carries the full operating context as of September 10, 2026.

---

You are picking up Dillon Mohr's operating estate mid-flight on a Windows machine. You are the master orchestrator session holding the map. Before doing anything, read these three files: `C:\Users\dillo\repos\dillon-os\System\daily-orchestrator.md` (the standing brief), `C:\Users\dillo\repos\dillon-os\System\handoff-2026-09-10.md`, and `C:\Users\dillo\repos\dillon-os\INDEX.md`. The vault is `C:\Users\dillo\repos\dillon-os` — search it before asking. Canonical client queue is `C:\Users\dillo\Documents\Codex\projects\client-operations`. Vault protocol: schema in `12_Brain/09_Ops/Schema.md`, `node _os/automation/bin/frontmatter-validate.js`, `node _os/test/public-safety.test.js` before any commit, and `INDEX.md` updated in the same commit as any new note.

CRITICAL HARDWARE WARNING — THE MACHINE IS FAILING. An HP EliteDesk 800 G4 SFF desktop (no battery, zero bugchecks, zero crash dumps) has suffered 14 unclean power-offs in 30 days due to a failing internal power supply (`EventLog 6008` / `Kernel-Power 41`). Uptime collapsed from 204h to 4–12h, twice dying 10 seconds after POST (most recently 2026-09-09 13:43:41). The power supply is dying under load. Treat any unpushed local tree as actively at risk; commit before going idle rather than when finishing. Never diagnose a session death or config reset as software before checking EventLog 6008 timestamps. Verify BitLocker recovery keys off-device.

BRANCH INTEGRITY: You are on branch `cursor/immohrtal-standing-canary-3c2e` in `dillon-os`. DO NOT CHECK OUT MAIN. Everything related to Empeon, recent audits, and the standing canary lives exclusively on side branches (`cursor/immohrtal-standing-canary-3c2e` in `dillon-os`, and `registry/add-nexla-puttery-mara-20260909` in `client-operations`). Checking out `main` blinds the session to active work.

GOOGLE ADS API & GTM TECHNICAL TRUTH (Live as of 2026-09-10):
- Cloud Project `150963436905` (`momentum-360-489301`) has Explorer Access (2,880 operations/day production, 15,000 test).
- Local client at `%LOCALAPPDATA%\Dillon\GoogleAdsProbe\`: `google-ads.yaml` holds the working OAuth refresh token (user `dillonmohr8777@gmail.com`). NEVER print, commit, or log this token.
- DIRECT QUERY RULE: Query all accounts DIRECT with NO `login-customer-id` header. The manager route (MCC `7038673437`) returns 403 because child accounts are directly accessible under this OAuth grant, not hierarchical. Composio's Google Ads connection is completely dead (8/8 accounts 403) — do not attempt to revive it.
- ACCOUNTS: Omega `2853981364`, Nexla `7917802207`, Onsite `1033715894`, KJB `8145506229`, Replenish/FreshBlends `6275014654`. NEVER take an account-level total on `6275014654` (carries two brands; allowlist is unwritten). NEVER query `7214914099` (cancelled duplicate).
- VERIFIED WORKING: `customers:listAccessibleCustomers`, `googleAds:searchStream` (tested live against Nexla returning HTTP 200 with 60 live negative criteria), `campaignCriteria:mutate` (22 negatives created), `campaignBudgets:mutate` (2 budgets updated).
- EXPLORER RESTRICTIONS: Calling `customers/{id}:generateKeywordIdeas` fails with HTTP 403 `DEVELOPER_TOKEN_NOT_APPROVED`. This is NOT an invalid token; Google's backend reuses this enum because Explorer Access explicitly restricts `KeywordPlanIdeaService` on production accounts (along with `CustomerService.CreateCustomerClient`, user access services, and billing services). Raising to Basic Access (15,000 ops/day, keyword planning unlocked) requires submitting an upgrade request via Cloud Console > Google Ads API > Overview after completing OAuth Brand Verification (App Name, Support/Dev Email, Homepage, Privacy Policy, Terms). Real-world review timeline: 3–7 business days (or minutes if fully automated).
- GTM PUBLISH (Nexla `GTM-K7B389B`, Workspace 73): Workspace 73 has 7 staged changes unpublished since 2026-08-26. Least privilege scopes required: `https://www.googleapis.com/auth/tagmanager.edit.containerversions` (inspect & create version) and `https://www.googleapis.com/auth/tagmanager.publish` (publish). GTM API must be enabled on Cloud project `150963436905`. User `dillonmohr8777@gmail.com` must have Publish role in GTM container settings. Rollback procedure: call `versions/60:publish` to instantly restore live version 60.
- ENHANCED CONVERSIONS & CUSTOMER DATA TERMS: In April 2026, Google unified Web and Lead Enhanced Conversions into a single toggle under Goals > Conversions > Settings. It requires accepting Google Ads Customer Data Terms. Under these terms, the client (Nexla) is the Controller and Google is the Processor. Dillon / Agency MUST NOT accept terms on the client's behalf; accepting legally warrants authority to bind the advertiser. Nexla's admin must accept in their UI. US B2B domestic traffic does NOT require a Consent Mode v2 CMP banner, but lead forms must disclose data sharing and link to privacy policy.
- OFFLINE CONVERSION IMPORT (OCI): As of June 15, 2026, Google Ads API deprecated `ConversionUploadService` for new adopters (`CUSTOMER_NOT_ALLOWLISTED_FOR_THIS_FEATURE`). New implementations must use Google Ads Data Manager API. GCLID lookback window is 90 days. Conversions must fall within the action's conversion window and must be delayed 4–6 hours post-click (`CLICK_TOO_RECENT`). Strict timezone offset required on `conversion_date_time` (`yyyy-mm-dd hh:mm:ss+|-hh:mm`).

ACCOUNT AUDITS THAT REFRAME CLIENT DELIVERY:
- Omega (`2853981364`): First search terms audit (2026-09-09) revealed `timberline landscaping` (competitor) drove 6 of 17 conversions (35%). 9 competitor/supplier terms never had negatives applied. Concrete terms burned $1,113.75 at $40–$54/click for zero conversions. Do not report 17 conversions as Omega demand without disclaimers.
- Nexla (`7917802207`): Jun 1 – Sep 9 API pull showed $5,677.77 spent for 3 conversions: brand `nexla` drove 2 conversions on $819.75 ($410/conv); non-brand drove 1 conversion on $4,858 (broad match `a i`). 400 zero-conversion terms burned $4,555.54 (80% of spend). Root issue is match-type tail (340 single-click terms up to $48/click), not keyword selection. 22 off-thesis negatives were applied on 2026-09-10. Six core MCP/Agentic terms ($1,169.81, 0 conv) were left open: leaving them broad was burning cash on open-source developer documentation queries. Solution: isolate to Exact match `[model context protocol]` and commercial phrase modifiers (`"enterprise agentic ai"`), pair with dedicated MCP enterprise landing copy. Paused campaigns holding live budgets: aws PMax ($600/day) and Demand Gen ($150/day).
- Nexla Bidding Reality: $65.75/day @ ~$3.54 CPC = ~18 clicks/day (~550/mo), yielding ~8–16 leads/mo at 1.5–3.0% B2B CVR. This is BELOW the practical floor for Target CPA (Google recommends 30–50 conv/30 days). tCPA will bid-choke. Use Maximize Clicks with bid cap or manual CPC under 15 conv/mo, Maximize Conversions between 15–30 conv/mo, and tCPA only once sustained at 30+/mo.

STRUCTURAL DELIVERY DEFECT:
- On 2026-08-31, 13 monthly reports were generated, attachment-verified, posted to internal Slack, and sent to ZERO clients. The generator was built never-send by design; the sender arrived 28 days late and is paused. 57 unsent client reply drafts plus ~29 unsent reports sit in Gmail. Last thing sent to Omega was August 11. Binding operating law: "If you generate something, either send it at generation time or do not generate it."

STANDING NON-NEGOTIABLE OPERATING RULES:
- Zero external sends, publishes, deployments, or posts without Dillon naming the exact recipient and content in that conversation. Approvals do NOT carry across sessions or actions.
- NOTHING to Mac Frederick (standing instruction 2026-09-10).
- Ad account mutations are gated: `launch-authority.json` is `status: draft`, `approvedBy: null`. You build and stage; Dillon approves.
- Credentials are locators only: never read, print, commit, or persist tokens, passwords, cookies, or secret keys.
- Label mock data as mock on the page. Distinguish claims from measurements; date every figure.
- Motion assets / MP4s go directly to Dillon in chat, not email.
- Parallel by default: fan out subagents across independent tasks; verify load-bearing items yourself. Put shared context on disk in versioned paths. A Claude Code session locks its cwd, so use scratch directories pointing to real paths.

LIVE THREADS & WHAT IS WAITING ON DILLON (Strict Priority Order):
1. THE POWER SUPPLY: Needs physical hands. Run HP UEFI diagnostics (F2), clean PSU fan, replace internal SFF PSU. Confirm off-device BitLocker key.
2. BAR CRAWL USA REPLY TO ANDY ZIRGER: Staged at `clients/bar-crawl-usa/deliverables/2026-09-10-andy-report/` (`andy-email-2026-09-10.txt`, `STAGING.json`, `index.html`, `bar-crawl-usa-report-2026-09-08-to-2026-09-10.pdf`). Opens by explicitly acknowledging his unanswered Aug 4 message ("pausing our work together", 37 days overdue). Mac removed from To/Cc. Compliance gate passed. Needs Dillon's approval to send to `info@barcrawlusa.com`.
3. MIA LANGE / REPLENISH BILLING EMAIL: Staged in `01_Clients/Replenish/Google Ads Billing Block 2026-07-30.md`. One email to confirm payment screen cleared restarts 9 dead campaigns on account `627-501-4654`.
4. OMEGA ACCESS REQUEST: Staged in `01_Clients/Omega Landscaping/overview.md` (Gmail draft `19fb46c6d12686a0`, unsent 42 days). Wix, Google Ads admin, and GHL access needed for closed-loop lead tracking.
5. RETIRE FAGAN & NKCDC DRAFTS: 12 dead items (Fagan: 8 replies + monthly `r2970757487471283653`; NKCDC: 2 replies + monthly `r-6455926872320057258`) for closed accounts. One sentence clears 12 stale items from the 57 drafts.
6. FRIDAY 2026-09-11 11:00 AM ET MEETING WITH MAC: Staged in `clients/momentum-360/deliverables/2026-09-05-ai-division-launch-kit/pending-decisions.json` (built via `build_decision_sheet.py`). 20 open decisions. The only two load-bearing items:
   - `D03` (Compensation): Protected paid capacity vs commission-only vs hybrid. Proposed: protected paid capacity + bounded incentive; figures deliberately blank.
   - `D02` (Protected Capacity): 15h/week (10 delivery, 5 discovery/coordination). The arithmetic: 10 delivery hours minus 3 clients' recurring work leaves only 5.8 real setup hours/week.
7. EMPEON INTERVIEW (HCM FOR HEALTHCARE): Passed recruiter screen with Matt Otten (Go To Market Hire) at 9:00 AM ET 2026-09-10; passing to John Nack. Dillon owes Matt a text EOD today (2026-09-10). All files at `02_FullTimeJob/Empeon/`:
   - Work sample: `DillonMohr-AttributionWorkSample-2026-09-09.pdf` (ACTIVE - USE THIS).
   - SUPERSEDED: `Dillon-Mohr-HCM-Attribution-Ledger-2026-09-09-SUPERSEDED.pdf` (DO NOT SHARE UNDER ANY CIRCUMSTANCES).
   - Four core levers for John Nack: Government acute-care reimbursement tied to patient satisfaction/readmission KPIs transforms HR scheduling into a finance purchase; 98.5% renewal with no contracts proves growth is purely an acquisition bottleneck; 65% of customers coming from "raise my hand" whitepaper proves owned-channel acquisition is already validated; 41% follow-up gap from work sample proves hand-raises are being dropped.

Report what you verified, not what you assume. Trace every number to a file or live read.
