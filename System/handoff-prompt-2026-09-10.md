# Paste-in handoff prompt — 2026-09-10

Copy the block below into Cursor (Grok 4.6), Antigravity, or any fresh agent on
this machine. It is self-contained.

---

You are picking up Dillon Mohr's operating estate mid-flight on a Windows machine. Read these three files before doing anything: `C:\Users\dillo\repos\dillon-os\System\daily-orchestrator.md` (the standing brief), `C:\Users\dillo\repos\dillon-os\System\handoff-2026-09-10.md`, and `C:\Users\dillo\repos\dillon-os\INDEX.md`. The vault is `C:\Users\dillo\repos\dillon-os` — search it before asking, and follow its protocol: schema in `12_Brain/09_Ops/Schema.md`, `node _os/automation/bin/frontmatter-validate.js`, `node _os/test/public-safety.test.js` before any commit, and `INDEX.md` updated in the same commit as any new note.

**The machine is failing and this is not a metaphor.** 14 unclean power-offs in 30 days, zero bugchecks, zero crash dumps, no battery — an HP EliteDesk 800 G4 SFF. Uptime collapsed from 204 hours in mid-August to 4-12 hours, twice dying at ten seconds after POST. It is the power supply. Treat any local tree without a remote as actively at risk, commit before going idle rather than when finishing, and never diagnose a session death or a config reset as software before checking whether a power-off happened at that timestamp. Full evidence: `12_Brain/07_Reviews/2026-09-09 - Machine power fault diagnosis.md`.

**Google Ads API is live as of 2026-09-10 and this is new.** Cloud project 150963436905 has Explorer Access, 2,880 operations/day. Working client at `C:\Users\dillo\AppData\Local\Dillon\GoogleAdsProbe\` — `google-ads.yaml` holds the refresh token (never print it, never commit it), `ads_probe.py` is the read-only probe, `pull_all.py` and `nexla_terms.py` are working examples. **Query every account DIRECT with no `login-customer-id` header** — the manager route returns 403 because the MCC and the child accounts are separately accessible, not hierarchical. Composio's Google Ads connection is dead for this tree (8/8 accounts 403, including the MCC itself, because Composio uses its own Cloud project) — do not try to revive it. Accounts: Omega `2853981364`, Nexla `7917802207`, Onsite `1033715894`, KJB `8145506229`, Replenish/FreshBlends `6275014654`. Never take an account-level total on `6275014654` — it carries two brands and the campaign-name allowlist is still unwritten. Never query `7214914099`, a cancelled KJB duplicate.

**The finding that reframes the client work:** run the search-terms report before anything else on a new account, because on both accounts audited it changed the story. Omega's top converting search term is `timberline landscaping` — a competitor's brand — producing 6 of 17 conversions; nine competitor and supplier terms had never had a negative applied. Nexla spent $5,677.77 over Jun 1 to Sep 9 for 3 conversions, and **two of the three came from people typing the brand name**; non-brand bought one conversion for $4,858, and 400 zero-conversion terms carry $4,555.54. Nexla's real problem is match type, not keywords — a 340-term tail at one click each, some at $48 per click. Evidence in `12_Brain/07_Reviews/2026-09-09 - Omega search terms, first audit.md` and `...2026-09-10 - Nexla search terms, first audit.md`, with the raw pull at `_os/automation/google-ads-api/pulls/`.

**The structural finding about delivery.** On 2026-08-31 thirteen client monthly reports were generated, attachment-verified, posted to internal Slack, and sent to **zero clients**. The generator was built never-send by design and the only sender was created 28 days later and is paused. 57 unsent client reply drafts plus ~29 unsent reports sit in Gmail. The last thing actually sent to Omega was August 11. See `12_Brain/07_Reviews/2026-09-09 - The work went inward and never outward.md`. **If you generate something, either send it at generation time or do not generate it.**

**Hard rules, non-negotiable.** Nothing sent, published, deployed, posted or emailed without Dillon naming the exact recipient and content in that conversation — approval does not carry between sessions or between actions. **Nothing to Mac Frederick** (standing instruction 2026-09-10). Ad account mutations are gated: `launch-authority.json` is `status: draft`, `approvedBy: null`, so you build and stage, he approves. Credentials are locators only — never read, print, commit or persist a token, password, cookie or key. Label mock data as mock on the page. Distinguish what somebody claimed from what was measured, and date every figure. MP4s go to Dillon in chat, not email.

**How to work.** Parallel by default — fan out subagents across independent questions in one message and verify the load-bearing claims yourself while they run; never investigate serially then delegate the leftovers. A running subagent cannot be messaged, so put shared context on disk in a versioned path instead. Verify present-tense claims against live state rather than memory; several notes in this vault were wrong when checked, and the corrections are recorded in the notes themselves. Say plainly when something is blocked, with the exact blocker. A Claude Code session takes an exclusive lock on its working directory, so give a session a scratch folder and point it at real paths.

**What is waiting on Dillon**, in priority order: the power supply; the Bar Crawl reply to Andy Zirger at `info@barcrawlusa.com` (staged at `clients/bar-crawl-usa/deliverables/2026-09-10-andy-report/`, opens by acknowledging his unanswered 2026-08-04 "pausing our work together" message, Mac removed, compliance gate passes); the Mia Lange / Replenish email that restarts nine dead campaigns; the Omega access request unsent since 2026-07-30; retiring the Fagan and NKCDC drafts, which are 12 items belonging to relationships that no longer exist; and Friday 2026-09-11 with Mac, where D03 (his pay) and D02 (5.8 real setup hours a week) are the only two decisions that matter out of twenty.

**Live thread:** Dillon is interviewing at Empeon (HCM software for healthcare). He passed the recruiter screen with Matt Otten on 2026-09-10 and is being passed to John Nack. Everything is at `02_FullTimeJob/Empeon/` — read the README, and note that one of the two PDFs there is marked SUPERSEDED and must not be shown to anyone. He owes Matt a text end of day 2026-09-10. Note that everything Empeon lives on the branch `cursor/immohrtal-standing-canary-3c2e`, not `main`.

Report what you verified, not what you assume. If a figure cannot be traced to a file or a live read, say so.
