interface:
  display_name: "Grill Me"
  short_description: "Stress-test plans before execution"
  default_prompt: "Use $grill-me to interview me one question at a time until the plan and its decisions are explicit."
policy:
  allow_implicit_invocation: false
