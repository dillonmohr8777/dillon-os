.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/plugins/obsidian-local-rest-api/data.json
08_Assets/
09_Transcripts/

# Large binaries never belong in git
*.zip

# Node
node_modules/
.next/
dist/

# Playwright QA output
_templates/site-factory/qa-shots/

# Harvested third-party reference material (screenshots, source imagery)
_templates/site-factory/harvest/

# 12_Brain private layer — GitHub repo is PUBLIC; keep PII/access/secrets out of Git
12_Brain/private/**
!12_Brain/private/README.md

# Public-safety scanner fixture: deliberately PII-shaped strings used to prove the
# scanner fires. Tests only assert its allowlist membership, never read it, so it
# stays local rather than shipping sample PII in a public repo.
_os/test/fixtures/public-safety-allowlisted.md
