# Report manifests

Place bounded manifests matching `12_Brain/schemas/report-run.json` here, then
validate and ingest them with `_os/automation/bin/report-ingest.js`.

Manifests contain exact routes and artifact paths only. Do not place raw report
data, credentials, communication archives, or client secrets in this folder.
