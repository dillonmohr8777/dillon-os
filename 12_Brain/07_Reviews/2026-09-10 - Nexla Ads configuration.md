# Nexla Ads configuration

2026-09-10. Client `nexla`. Google Ads `7917802207`.

The live Brand and MCP campaigns were optimizing a custom goal that only contained CRM Lead uploads. That goal now contains only `HubSpot-Demo Request`. CRM lifecycle actions are Secondary. Thirty-one paused budgets that summed to $1,440/day are disarmed to $1. Brand $25 and MCP $40.75 are still the only live spend.

GTM workspace 73 is still unpublished. The 3-ad-group RSA rebuild stays draft.

Package: `client-operations/clients/nexla/deliverables/2026-09-10-ads-config/`
